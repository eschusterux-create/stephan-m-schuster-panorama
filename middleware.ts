// No locale-based URL routing – German is the primary language.
// next-intl reads locale from i18n.ts (defaults to 'de').
export function middleware() {
  // passthrough
}

export const config = {
  matcher: [],
};
